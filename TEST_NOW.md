# 🧪 立即测试 - 移动端修复验证

## 快速测试步骤（5分钟）

### 1️⃣ 选中节点 (30秒)

**操作**: 轻触节点主框（图片/视频显示区域）

**预期**:
- ✅ 节点显示蓝色边框
- ✅ 控制面板出现在节点下方

**如果失败**: 
- 检查是否点击了标题或控制面板
- 尝试点击节点中心区域

---

### 2️⃣ 拖拽节点 (30秒)

**操作**: 触摸节点主框并移动手指

**预期**:
- ✅ 节点跟随手指移动
- ✅ 释放后停留在新位置

**如果失败**:
- 确认触摸的是主框，不是标题或控制面板
- 尝试按住1秒再移动

---

### 3️⃣ 输入文字 (1分钟)

**操作**: 
1. 选中节点
2. 点击 Prompt 输入框
3. 输入 "test"

**预期**:
- ✅ 键盘弹出
- ✅ 可以输入文字
- ✅ 节点不移动

**如果失败**:
- 打开浏览器控制台查看错误
- 确认输入框有 `data-interactive="true"`

---

### 4️⃣ 点击按钮 (30秒)

**操作**: 点击 Generate 按钮

**预期**:
- ✅ 按钮有视觉反馈
- ✅ 触发生成（如果配置了API）
- ✅ 节点不移动

**如果失败**:
- 检查按钮是否在控制面板内
- 确认控制面板有 `onTouchStart={(e) => e.stopPropagation()}`

---

### 5️⃣ 下拉菜单 (30秒)

**操作**: 点击 Model 下拉菜单

**预期**:
- ✅ 菜单展开
- ✅ 可以选择选项
- ✅ 节点不移动

---

### 6️⃣ 创建多个节点 (1分钟)

**操作**: 
1. 从侧边栏添加新节点
2. 选中新节点
3. 拖拽新节点

**预期**:
- ✅ 每个节点都可以选中
- ✅ 每个节点都可以拖拽
- ✅ 功能区都可以使用

---

### 7️⃣ 连接节点 (1分钟)

**操作**:
1. 从节点A的输出端口拖动
2. 连接到节点B的输入端口

**预期**:
- ✅ 连接线创建成功
- ✅ 可以继续创建更多连接

---

## 🔍 调试技巧

### 如果所有功能都不工作

1. **刷新页面**
2. **清除缓存**
3. **检查控制台错误**

### 如果只有功能区不工作

在控制台运行：
```javascript
// 检查功能区元素
const panel = document.querySelector('.absolute.top-full');
console.log('Panel found:', !!panel);
console.log('Has data-interactive:', panel?.hasAttribute('data-interactive'));
console.log('Has onTouchStart:', panel?.ontouchstart !== undefined);
```

### 如果只有拖拽不工作

在控制台运行：
```javascript
// 检查主框元素
const handle = document.querySelector('[data-drag-handle="true"]');
console.log('Handle found:', !!handle);
console.log('Has onTouchStart:', handle?.ontouchstart !== undefined);
```

---

## ✅ 成功标准

所有7个测试都通过 = 修复成功！

如果有任何测试失败，请查看 `MOBILE_DEBUG_GUIDE.md` 进行详细调试。

---

**预计测试时间**: 5分钟  
**建议设备**: iPhone 或 Android 手机
